# 功能与业务逻辑说明

## 塔罗结构

- 大阿卡纳：22 张，编号 0–21，代表较大的主题与人生阶段。
- 小阿卡纳：56 张，分为权杖、圣杯、宝剑和星币。
- 每种花色：Ace 至 10，加侍从、骑士、王后和国王，共 14 张。

## 牌库浏览

- 默认展示当前牌组的全部 78 张牌。
- 可按大／小阿卡纳、花色、编号和关键词筛选。
- 搜索同时匹配中文名、英文名和关键词。

## 抽牌规则

1. 每次开始前生成当前牌组的副本并随机洗牌。
2. 同一次抽牌中不允许重复出现同一张牌。
3. 开启逆位后，每张牌独立生成正位或逆位状态。
4. 翻牌前不展示牌名和牌面。
5. 结果只连接到资料，不自动生成结论。

## 历史记录

每条记录包含时间、牌组、牌阵、用户问题、抽到的牌、正逆位和用户笔记。第一版使用 `localStorage` 保存在当前设备。

## 异常处理

- 图片加载失败时显示牌名和统一占位图。
- 数据字段缺失时隐藏对应区域，不显示 `undefined`。
- 本地存储不可用时仍允许抽牌，并提示记录无法保存。
